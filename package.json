{
  "name": "planet-bounce",
  "private": true,
  "version": "0.1.0",
  "description": "A mobile-first gravity adventure: bounce from planet to planet using nothing but momentum and gravity.",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "license": "MIT",
  "devDependencies": {
    "playwright-core": "^1.63.0",
    "typescript": "~5.9",
    "vite": "^8.3.1",
    "vitest": "^5.0.1"
  }
}
